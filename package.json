{
  "name": "node_typescript",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf ./build && npx tsc",
    "start": "yarn run build && node -r tsconfig-paths/register build/index.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.32",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.3"
  }
}
